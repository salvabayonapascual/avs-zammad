title = "gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Archivos ya cifrados (alta entropia por diseno) y ejemplos de documentacion con placeholders, no secretos reales"
paths = [
  '''.*\.enc$''',
  '''\.skills/zammad-api/SKILL\.md''',
]
